---
title: リファレンスゲーム
description: Dot コレクションで作られた 5 本のゲーム。それぞれが何を証明するために存在するのか、そしてアドオン間の継ぎ目がゲームの中でしか動かない理由。
navTitle: リファレンスゲーム
order: 50
keywords:
  - "サンプル"
  - "例"
  - "デモ"
  - "game-arena"
  - "game-hungario"
  - "game-playground"
  - "game-g2gfast"
  - "ロビー"
  - "デスマッチ"
  - "サンドボックス"
source:
  - ../game-dev/godot/NOTES.md
  - ../game-dev/CLAUDE.md
---

5 本のゲームがあり、これらはデモではありません。各アドオンのテストスイートは
**他のアドオンが無い状態**でそのアドオンを走らせるので、アドオン同士の接合は
ゲームの中にしか存在しません。そしてこのコレクションが繰り返し学んだ教訓の通り、
*1 つのデプロイ形態からしか到達しないコードパスは、何も実行していないコードパス*
です。

<Callout type="info" title="バグはここにあります">
ゲームプレイ系アドオンを 1 プロセスに通したところ 7 件のバグが見つかり、うち 6 件は
*他の*リポジトリのもので、そのリポジトリ自身のスイートからは 1 件も到達できません
でした。全体を通しで動かすとさらに 12 件。実ブラウザークライアントを実サーバーに
つないでさらに 7 件。この傾向は毎回同じです。
</Callout>

## game-arena

**開発用テクスチャーの 3D アリーナ・デスマッチ。リファレンスゲームです。**
アドオンが組み合わさることを示し、橋渡しコードがどんな形になるかを見せ、新しい
ゲームがそこからコピーされる対象になるために存在します。

`dot-core`、`dot-fps-controller`、`dot-combat`、`dot-loadout`、`dot-match`、
`dot-ui`、`dot-server`、`dot-net` を使います。

重要なファイルは 4 つで、それぞれがアドオンではなく*ゲーム側*にある理由は毎回
同じです。

- **`maps/arena_map.gd`** — レベルは箱のリストで、そのリストが 3 つのものになります。
  メッシュ、物理ボディ、そしてヘッドレスサーバー用の解析ジオメトリー。別々に作ると
  3 つの記述がずれていき、そのずれはクライアントに見えている何かを弾がすり抜け
  始めるまで気づけません。
- **`game/arena_player.gd`** — 移動、武器、体力、ヒットボックスを 1 つのボディに。
  どのアドオンも「この配線はゲームの仕事だ」と言います。アドオンがやってしまえば
  シーンの形を押し付けることになるからです。
- **`game/arena_game.gd`** — 継ぎ目。6 つのアドオンはそれぞれ単体では正しく、ここが、
  戦闘のキルが試合のスコアになり、試合のリスポーンがロードアウトになる 50 行です。
- **`game/arena_module.gd`** — `dot-server` を名指しする唯一のファイル。約 40 行で、
  これが専用サーバー統合のすべてです。

武器が 4 つなのは、「どうやって距離を詰めるか」への本当に異なる 4 つの答えだから
です。これより少ないデスマッチには正解が 1 つしかありません。ロードアウトは 6
ポイント予算で武器 2 丁——列挙ではなく 4 つの数値です。

```bash
godot --headless --path . res://examples/headless_match.tscn   # デスマッチを丸ごと
godot --headless --path . res://examples/headless_net.tscn     # サーバー + クライアント、20% パケットロス
godot --headless --path . res://examples/dedicated.tscn        # 実物の DotServer
```

## game-hungario

**食べて大きくなるアリーナゲーム。操るのは怪物で、その上にアバターが乗っています。**
コレクション初の遊べるクライアントであり、実ソケット上でネットコードが動いた最初の
ゲームであり、ゲームプレイ側と身元側が一緒に走る唯一の場所です。

`dot-2d`、`dot-net`、`dot-server`、`dot-match`、`dot-ui`、`dot-user-avatar`、
`dot-loadout`、`dot-platform`、`dot-cloud` を使います。`dot-fps-controller` と
`dot-combat` 以外すべてです。

これは **ブラウザー対応への最短経路**でもあります。小さく、スレッドも不要で、
ほかに邪魔するものが無い状態でブラウザーの制約をひと通り踏みます。
`web/README.md` がデプロイの契約書です。ブラウザーに UDP が無いので WebSocket、
タブは待ち受けできないのでクライアント専用、HTTPS ページは非セキュアなソケットを
開けないので `wss://`、そしてスレッドサポートは無効——有効にすると全レスポンスに
クロスオリジン分離が必要になり、ページ上のサードパーティー埋め込みがすべて壊れる
からです。

## game-playground

**古典的な物理サンドボックスの形をしたサンドボックス。** Q を押しっぱなしにすると
14 種のプロップ、スクリプト付き NPC 4 体、武器 3 種のメニューが出て、クリックすれば
自分のものになります。掴んで凍らせるフィジックスガン、吹き飛ばすグラビティガン。
マップ 3 つ、専用サーバー、コンソールコマンド 20 個、そして `pg_lobby` のボーナス
トラックにある 9 枚のプラットフォームのジャンプコース——ジャンプコースは移動系
ジャンルではなく、[`dot-timer`](/learn/godot/addons/dot-timer) が surf と bunny-hop
専用ではないことを示すものだからです。

`dot-fps-controller`、`dot-timer`、`dot-map`、`dot-props`、`dot-leaderboard`、
`dot-ui`、`dot-server` を使います。

<Callout type="warning" title="エンティティと武器はパスで指定されたスクリプトです">
`class_name` ではありません。マウントされた `dot-cloud` パックは `class_name` の
グローバルを登録できません——計測済みです——ので、パックがマウントされ、そのシーンが
読み込まれる一方で、パック内のファイルをまたぐ型参照はすべてコンパイルに失敗します。
**パスこそ、配信されるエンティティが取りうる唯一の形**であり、このゲームは意図的に
そう書かれています。
</Callout>

`dot-fps-controller`、`dot-timer`、`dot-map`、`dot-props`、`dot-leaderboard`、
`dot-ui`、`dot-server` が 1 プロセスで動く唯一の場所です。このスイートは 9 件の
バグを見つけ、うち 3 件は他のリポジトリのものでした。スクリーンショットがさらに
4 件を見つけています。

## game-g2gfast

**Counter-Strike の形をした、bunny-hop と surf のタイマーサーバー。** Source の
単位と cvar、Source の視野角、一人称と三人称、そして全プレイヤーがアバターとして
描かれます——プラットフォームから来た本人のもの、あるいは既定のもの。

`dot-fps-controller`、`dot-timer`、`dot-map`、`dot-leaderboard`、`dot-server`、
`dot-user-avatar`、`dot-ui` を使います。

```
sv_tickrate 100
sv_autobunnyhopping 1
sv_airaccelerate 1000        // surf なら 150
sv_gravity 800
sv_allow_thirdperson 1
sv_replay_bot 1              // サーバー記録を見えるゴーストとして走らせる
```

そしてコンソールから、作者がこのエンジンを一度も使ったことのないマップにゾーンを
引きます。

```
g2g_zone start
g2g_zone_mark          // 片方の角に立つ
g2g_zone_mark          // もう片方の角に立つ
g2g_zone stage main 1
g2g_zone_save
```

## game-simple-lobby

**ロビーです。** チャットと在室者一覧と入退室の通知がある小さな 2D の部屋——
サーバーが他に何も動かしていないときに動かすもので、入場・在籍・レプリケーション・
予測・チャットをまだ全部使う、最小のものです。

ゲームを読み込んでいないサーバーは正当な運用形態で、`dot-server` は昔から対応して
いました。無かったのは「見るもの」で、接続して何も見えないプレイヤーには、それが
壊れたサーバーと区別できません。汎用ブラウザークライアントが最初にダウンロードする
ゲームでもあります。

## 動かして分かったこと

繰り返し出てくる教訓が 4 つあり、自前の橋渡しを書く前に知っておく価値があります。

**正しく作られて誰にも読まれていない値は、間違って作られた値と見分けがつきません。**
補間された位置、関心集合、コンテンツクライアントは、いずれも正しく計算されていて
誰にも読まれておらず、どの場合も目に見える症状は生産側を指していました。

**2 つのゲームが読んでいて誰も発行していない値は、端を入れ替えた同じバグです。**
`DotTimer.effect_requested` は宣言され、転送され、接続されていて——どの行もそれを
発行していませんでした。その結果、2 本のゲームで各マップの落下用ボリュームはただの
飾りでした。

**インターフェースのバグはアサーションからは見えません。** プロパティはすべて
正しく、何も失敗しないからです。0 × 0 の `Control` ツリー 2 つと真っ黒な画面は、
テストではなく、フレームを 1 枚描いて目で見て見つかりました。

**動きも同じです。** どのスイートのどの検査も*シミュレーション上の*値を主張して
おり、ブラウザークライアントを遊べなくしていた揺れでも、シミュレーションされた数値は
すべて正しいままでした。バグはシミュレーションから画面への写像にあり、シミュレー
ションについてのどんなアサーションもそこには届きません。

<Related>
- [アドオン一覧](/learn/godot/addons)
- [拡張ポイント](/learn/godot/extending)
- [ツール](/learn/godot/tooling)
</Related>
